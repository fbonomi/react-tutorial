import React, { Component } from 'react';
import { Panel } from 'react-bootstrap';

class UsersFilter extends Component {
    render() {
        return (
            <Panel header="Filters">
                Sono la parte di filterssss
            </Panel>
        );
    }
}

export default UsersFilter;